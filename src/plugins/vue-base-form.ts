import Vue from "vue";
import Vuetify from "vuetify";
import { VTextField, VTooltip, VRow, VCol, VBtn, VCheckbox } from "vuetify/lib";
Vue.use(Vuetify, {
  components: { VTextField, VTooltip, VRow, VCol, VBtn, VCheckbox },
});

// Vue.component("v-html-label", () =>
//   import("@/modules/auth/components/terms-field.vue")
// );
